remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have on
title: Oscams build by ....
description: Oscams build by ....
show_downloads: true ["true" or "false" (unquoted) to indicate whether to provide a download URL]
